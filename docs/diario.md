# Diário de Desenvolvimento — Mandelbrot

Preenchido **ao longo** do desenvolvimento (não retroativamente), mesmo padrão usado em `processflow` (Implementação 1). Serve de matéria-prima para quando o relatório for escrito por fora. Datas/horas são reais (saída de `date` no WSL), não estimadas.

Plano completo em `C:\Users\arthu\.claude\plans\c-users-arthu-downloads-implementa-o-2-effervescent-pond.md`.

## Estratégias

| Estratégia | Nome curto | Contexto |
|---|---|---|
| S1 | Núcleo comum único | `common.c` concentra pixel→complexo, iteração de escape e normalização; as 4 implementações chamam as mesmas funções, só o laço externo (divisão de trabalho) muda — garante as 4 imagens idênticas exigidas pelo enunciado |

## Diário de tentativas

| # | Estrat. | O que tentei | Resultado | Hipótese/Causa (se falhou) | Quando | Evidência |
|---|---|---|---|---|---|---|
| 1 | S1 | Fase 0: scaffold do projeto (`.gitignore`, `README.md`, `Makefile` com `wildcard src/*.c`, `src/main.c` placeholder que só retorna 0) + copiar os 3 casos oficiais do professor (`testes (1).tar`, na verdade um `.zip`) para `tests/oficiais/` | OK — `make clean && make` compilou sem warnings de primeira; `./mandelbrot` rodou com stdout vazio e exit code 0 | — | 02/09/2026 15:30 (WSL2 Ubuntu, gcc 15.2.0) | `make` e `./mandelbrot` executados dentro do WSL2, ver histórico de comandos da sessão |
| 2 | S1 | Antes de escrever qualquer código: validar empiricamente a fórmula de mapeamento pixel→complexo, o critério de escape e a normalização contra os 3 casos oficiais, testando 16 combinações plausíveis (centro-do-pixel vs borda-do-pixel vs endpoint, dois critérios de escape, 5 fórmulas de normalização) num script Python fora do repo | A primeira leva (pixel-centro + arredondamento, que era a suposição inicial do plano) não bateu com nenhum dos 3 casos. Ampliando a busca, "borda do pixel" (`px/largura`, sem `+0.5`) + critério de escape clássico + normalização por divisão inteira TRUNCADA (sem arredondar) bateu 112/112 valores nos 3 casos | A suposição inicial (centro do pixel + `round()`) era plausível mas errada — o professor usa a borda do pixel e trunca em vez de arredondar na normalização | 02/09/2026 (antes da Fase 0, durante o planejamento) | Script de verificação (fora do repo, `scratchpad/verify2.py`); plano atualizado com a fórmula correta antes de qualquer `.c` ser escrito |
| 3 | S1 | Fase 1: `mandelbrot_parse_args` em `common.c` (via `strtol` + checagem de `endptr`/`ERANGE`/limites de `int`/positividade) + `main.c` chamando ela; escrevi junto `tests/casos_invalidos.sh` com os 14 casos de parsing da tabela de erros do plano (argc incorreto, não-numérico, lixo à direita, vazio, zero, negativo, overflow) | OK de primeira — `make clean && make` sem warnings, e os 14 casos do script bateram todos (`stdout` vazio, `exit!=0`, mensagem coerente em stderr) já na primeira execução | — | 02/09/2026 15:3x (WSL2 Ubuntu, gcc 15.2.0) | `bash tests/casos_invalidos.sh ./mandelbrot` — 14/14 OK; caso válido `10 10 50 4` conferido à parte: stdout vazio, exit 0 |
| 4 | S1 | Fase 2: `mandelbrot_pixel_to_complex`, `mandelbrot_escape_iterations`, `mandelbrot_write_pgm`, `mandelbrot_buffer_pixel_count` em `common.c` (fórmula da borda do pixel, validada no item 2). Laço serial provisório em `main.c` rodando `4 4 50 1` e comparando com `tests/oficiais/teste1_serial.txt` via `diff` | **Falhou na 1ª tentativa**: os 16 valores de intensidade batiam exatamente, mas o `diff` acusou diferença na última linha — `teste1_serial.txt` não tem newline no final do arquivo, e meu `mandelbrot_write_pgm` escrevia um `\n` depois de toda linha, inclusive a última | `\n` estava sendo tratado como terminador de linha, não como separador — o arquivo de referência do professor termina sem newline final | 02/09/2026 15:4x | `diff /tmp/esperado_teste1.txt mandelbrot_afsr_serial.pgm` mostrou `4c4` + `\ No newline at end of file` do lado esperado |
| 5 | S1 | Corrigido: `mandelbrot_write_pgm` passou a escrever `\n` ANTES de cada linha exceto a primeira (separador entre linhas, não terminador) | OK — `diff` limpo, "TESTE1: IDENTICO"; regressão dos 14 casos inválidos + caso mínimo `1 1 1 1` (não trava, produz `255`, coerente com `max_iter=1`) continuam OK | — | 02/09/2026 15:4x | `diff` sem saída; `bash tests/casos_invalidos.sh` 14/14 OK de novo |
| 6 | S1 | Fase 3: extraí o laço provisório da Fase 2 pra `serial.c` (`mandelbrot_run_serial`, cronometrado com `clock_gettime(CLOCK_MONOTONIC)`); `main.c` passou a orquestrar de verdade (aloca buffer uma vez, chama a implementação, escreve `.pgm`, escreve `times.txt` com `mandelbrot_write_times` — OpenMP/Pthreads1/Pthreads2 ainda em `0.000000s`, só entram nas Fases 4-6) | OK de primeira — build sem warnings, `teste1_serial.txt` continua idêntico, `times.txt` parcial no formato certo (`Serial: 0.000001s`, bem próximo do exemplo do professor), regressão dos 14 casos inválidos OK | — | 02/09/2026 15:5x | `diff` limpo; `cat times.txt`; `bash tests/casos_invalidos.sh` 14/14 OK |
| 7 | S1 | Fase 4: `openmp.c` (`mandelbrot_run_openmp`, `#pragma omp parallel for schedule(static) num_threads(p->num_threads)` sobre as linhas, chamando as mesmas funções de `common.c`; cronometrado com `omp_get_wtime`); `main.c` passou a chamar as duas implementações em sequência | OK de primeira — build sem warnings; `teste2_openmp.txt` (`6 6 30 3`) idêntico; `serial == openmp` na mesma rodada; `teste1_serial.txt` (regressão) continua idêntico; rodei `6 6 30 <n>` com `num_threads` = 1, 3 e 8 (> altura=6) e as 3 saídas ficaram idênticas entre si; 14/14 regressão de argumentos inválidos OK | — | 02/09/2026 16:0x | `diff` limpo nos dois testes oficiais; `diff -q` entre as 3 saídas com `num_threads` diferentes |
| 8 | S1 | Fase 5: `pthreads1.c` (`mandelbrot_run_pthreads1` — divisão estática por blocos: `base=altura/n`, `resto=altura%n`, primeiras `resto` threads recebem 1 linha a mais; fórmula cobre `num_threads > altura` sem `if` especial; limpeza com `pthread_join` só nas threads já criadas se `pthread_create` falhar no meio, mensagem via `strerror(rc)`) | OK de primeira — build sem warnings; `teste3_pthreads1.txt` (`10 6 40 4`) idêntico; `serial == openmp == pthreads1` na mesma rodada; regressão de `teste1`/`teste2` OK; `num_threads` = 1, 6(=altura) e 9(>altura) deram resultado idêntico entre si; 14/14 argumentos inválidos OK | — | 02/09/2026 16:1x | `diff` limpo no oficial + regressões; `diff -q` entre as 3 saídas com `num_threads` diferentes |
| 9 | S1 | Rodei uma build separada com `-fsanitize=thread` (ThreadSanitizer, só verificação — não entra no Makefile de entrega) sobre as 4 implementações existentes até agora | **TSan acusou uma "data race"**: leitura em `mandelbrot_write_pgm` (`common.c:126`, thread principal) vs escrita em `mandelbrot_run_openmp` (`openmp.c:13`, dentro do `#pragma omp parallel for`) | Duas hipóteses possíveis: (a) bug real — falta alguma sincronização entre o fim da região `omp parallel for` e a leitura do buffer; (b) falso positivo conhecido do TSan, que não enxerga a barreira implícita interna do `libgomp` como uma aresta de *happens-before* | 02/09/2026 16:2x | Saída completa do TSan, ver `scratchpad/build_and_check_fase5.sh` da sessão |
| 10 | S1 | Investiguei a hipótese (a) antes de descartar: (1) testei com `OMP_WAIT_POLICY=passive` (troca o *spin-wait* do `libgomp` por futex, que o TSan intercepta melhor) — o aviso continuou idêntico; (2) escrevi um programa mínimo isolado (`omp_minimal_test.c`, ~10 linhas: `#pragma omp parallel for` escrevendo num array, seguido de leitura serial do array logo depois do pragma) e rodei sob TSan | O programa mínimo — um padrão OpenMP canônico, usado em qualquer tutorial — **também** acusa a mesma "race", com ou sem `OMP_WAIT_POLICY=passive` | Se até o padrão mais básico de OpenMP (escrever num `parallel for` e ler depois, sem nenhuma sincronização manual — a barreira implícita do `parallel for` é responsabilidade do runtime) dispara o aviso, é limitação do TSan em reconhecer a sincronização interna do `libgomp` nesta combinação gcc 15.2.0/TSan, não um bug de sincronização no meu código. Reforça isso: a mesma build de TSan **não** acusou nada no `pthreads1.c` (sincronização manual via `pthread_join`, que o TSan intercepta de forma confiável) — só a fronteira do `omp parallel for` fica sem cobertura. Decisão: não adicionar sincronização manual redundante ao redor do `#pragma omp parallel for` (a barreira implícita já é suficiente e correta pela especificação OpenMP); documentar a limitação e seguir usando TSan como checagem real só nas Fases com pthreads manual (Pthreads1 já OK; Pthreads2 na Fase 6) | 02/09/2026 16:3x | `scratchpad/investigate_tsan.sh` — reprodução mínima + teste da hipótese `OMP_WAIT_POLICY` |
| 11 | S1 | Fase 6: `pthreads2.c` (`mandelbrot_run_pthreads2` — fila circular limitada, 1 thread despachante que só enfileira índices de linha (nunca calcula pixel) + `num_threads` trabalhadoras que desenfileiram e calculam; mutex + `not_empty`/`not_full` condvars; `while` nas esperas (não `if`); `signal` em "produzi 1"/"consumi 1", `broadcast` em "acabou"/"aborta"; flag `abort_flag` própria para destravar threads já criadas se `pthread_create` falhar no meio) | OK de primeira funcionalmente — build sem warnings; as 4 implementações batem entre si na mesma rodada (`10 6 40 4`); os 3 casos oficiais (regressão) continuam idênticos; `pthreads2` com `num_threads` = 1, 4, 6(=altura) e 9(>altura) numa imagem maior (`50 37 300`) deu resultado idêntico entre si; 14/14 argumentos inválidos OK | — | 02/09/2026 16:4x | `diff` limpo em tudo; `diff -q` entre `num_threads` diferentes |
| 12 | S1 | Rodei o `pthreads2.c` (sincronização manual de verdade, diferente do OpenMP) sob ThreadSanitizer — 32 execuções (8 rodadas × `num_threads` ∈ {1,2,5,8}, imagem `30 23 150`) — pra tentar pegar alguma race intermitente no mutex/condvar da fila | Nenhum warning do TSan mencionou `pthreads2.c` em NENHUM frame nas 32 execuções (`grep -c pthreads2.c` sobre a saída completa = 0). O único warning que ainda aparece (esperado, mesmo caso da entrada 9/10) é sempre o mesmo falso positivo do `openmp.c`, já investigado e documentado — nunca envolve `pthreads2.c` | — | 02/09/2026 16:5x | `scratchpad/tsan_fase6_precise.sh` — checagem explícita de que `pthreads2.c` não aparece em nenhum stack trace reportado |
| 13 | S1 | Fase 7: escrevi `tests/rodar_oficiais.sh` (parseia os 3 `.txt` do professor por prefixo de rótulo — "Comando:" vs "Comando de entrada:", "Arquivo" vs "Arquivos a verificar:" — roda o comando, compara com `Conteudo esperado:`) e rodei contra o binário real | OK de primeira — os 3 casos oficiais (`teste1_serial`, `teste2_openmp`, `teste3_pthreads1`) OK | — | 02/09/2026 17:0x | `bash tests/rodar_oficiais.sh ./mandelbrot` — 3/3 OK |
| 14 | S1 | Tentei forçar falha real de `malloc`: `ulimit -v 32768` (32MB) + `./mandelbrot 20000 20000 10 1` (buffer pedido ≈1,6GB) no binário real | OK de primeira — `mandelbrot: falha ao alocar buffer de iteracoes` em stderr, stdout vazio, `exit=1`, sem crash | — | 02/09/2026 17:1x | `scratchpad/investigate_resource_limits.sh` |
| 15 | S1 | Tentei forçar falha real de `pthread_create` no binário completo via `ulimit -u` (nº de processos/threads do usuário): medi o uso atual (`ps -eLf \| wc -l` ≈ 93-94), defini o limite pouco acima (+8) e pedi `num_threads=200` | O programa terminou com `exit=1`, stdout vazio (bom), mas a mensagem em stderr foi `libgomp: Thread creation failed: Resource temporarily unavailable` — **não é a minha mensagem**, é o próprio runtime do OpenMP abortando o processo inteiro antes de qualquer uma das minhas 3 primeiras implementações (serial/openmp/pthreads1) devolver o controle pro meu código, já que openmp roda primeiro na sequência do `main.c` | Confirmado que não é crash (verifiquei: `exit=1` é `exit()` normal, não morte por sinal) — mas o runtime da OpenMP, ao não conseguir criar a equipe de threads sob exaustão extrema de recursos, chama sua própria rotina fatal (`gomp_fatal`) e encerra o processo direto, sem essa falha nunca chegar a ser "vista" pelo meu código (não existe um jeito padrão/portável de interceptar isso de dentro de um `#pragma omp parallel for` comum) | 02/09/2026 17:2x | `scratchpad/investigate_pthread_create_fail.sh` |
| 16 | S1 | Como o `ulimit -u` faz o OpenMP falhar primeiro (e o `num_threads` é compartilhado pelas 4 implementações numa única execução), não dava pra observar o `pthread_create` do MEU código (pthreads1.c/pthreads2.c) através do binário completo. Escrevi um harness isolado fora do repo (`test_pthreads_direct.c`) que chama `mandelbrot_run_pthreads1`/`mandelbrot_run_pthreads2` direto, sem OpenMP no caminho, e tentei de novo com `ulimit -u`: **não reproduziu** (500 threads criadas com sucesso mesmo com limite de nproc apertado — provavelmente minha medição via `ps -eLf` não reflete o contador real de NPROC por UID que o kernel usa). Troquei a estratégia para `ulimit -v 102400` (100MB) + `num_threads=2000` (cada pthread teria stack padrão de ~8MB — 2000 threads pediriam ~16GB, bem acima do limite de memória virtual) | **Essa reproduziu de verdade**: pthreads1 → `status=-1`, stderr `mandelbrot: pthreads1: pthread_create: Resource temporarily unavailable`; pthreads2 → `status=-1`, stderr `mandelbrot: pthreads2: pthread_create (worker 11): Resource temporarily unavailable` — ou seja, o despachante + 10 trabalhadoras foram criadas com sucesso, a 11ª falhou, e a limpeza (`abort_flag` + `broadcast` nas duas condvars + `join` só nas threads já criadas) funcionou sem travar — confirma na prática o caminho mais delicado do projeto (falha de criação NO MEIO da criação das threads do Pthreads2) | — | 02/09/2026 17:3x | `scratchpad/run_pthreads_direct_test2.sh` |
| 17 | S1 | Fechei a Fase 7: adicionei o caso de `malloc` forçado (`ulimit -v 32768` + `20000 20000 10 1`) como caso automatizado dentro de `tests/casos_invalidos.sh` (o de `pthread_create` fica só documentado no diário, pelo motivo da entrada 15 — não dá pra isolar de forma confiável contra o binário completo); montei o alvo `test` definitivo no `Makefile` (roda `320 241 200 4`, compara os 4 `.pgm` entre si, roda `tests/rodar_oficiais.sh`, roda `tests/casos_invalidos.sh`); rodei `make clean && make test` do zero | OK de primeira, tudo verde: as 4 implementações idênticas entre si; os 3 oficiais idênticos; 14 casos inválidos de parsing + o caso de malloc forçado, todos OK; `times.txt` com tempos reais pra imagem `320×241` (paralelas entre 0.005-0.0053s vs 0.0102s da serial — speedup real e plausível) | — | 02/09/2026 17:4x | `scratchpad/run_make_test.sh` — `make test` completo, exit 0 |
| 18 | S1 | Verificação final de reprodutibilidade e memória (mesmo padrão do `processflow`, Fase 8 dele): `git clone` do repo (no commit atual) pra uma pasta limpa em `/tmp` + `make test` do zero, sem nada do ambiente de desenvolvimento; depois `make clean` + `git status --short` pra confirmar que nada escapou do `.gitignore`; depois build separada com `-fsanitize=address,undefined` rodando 6 combinações de tamanho/`num_threads` (incluindo `1 1 1 1` e `num_threads` maior que a altura) | OK em tudo: clone limpo reproduziu `make test` idêntico (mesmos 3 oficiais, mesmos 14+1 casos inválidos); `git status --short` saiu vazio depois do `make clean`; as 6 execuções sob ASan+UBSan terminaram com `exit=0` e zero relatos de sanitizer (sem vazamento, sem UB, sem estouro de buffer) | — | 02/09/2026 17:5x | `scratchpad/repro_and_sanitizers.sh` |
| 19 | S1 | Fase 9 (empacotamento): `git archive --format=tar --prefix=afsr/ HEAD` extraído direto pra `D:\Repositorios\CESAR\afsr\` (pasta irmã do repo `mandelbrot/`, sem `.git`, só o que está versionado — Makefile, `src/`, `tests/`, `docs/diario.md`, `README.md`); depois `tar -cf afsr.tar afsr/`; prova final: extraí o `afsr.tar` gerado numa pasta totalmente separada (`/tmp/afsr_extraido`) e rodei `make test` de dentro dela, como se fosse exatamente o que o professor vai receber e descompactar | OK — `afsr/` contém exatamente os 20 arquivos esperados (Makefile, 12 arquivos em `src/`, 5 em `tests/`, README, diário, `.gitattributes`/`.gitignore`); `afsr.tar` com 71.680 bytes; `make test` rodado a partir do `.tar` extraído em pasta separada reproduz tudo igual (3 oficiais OK, 14+1 inválidos OK, 4 implementações idênticas) | — | 02/09/2026 18:0x | `scratchpad/package_afsr.sh` |
| — | S1 | Ao montar o checklist de requisitos do relatório, notei uma lacuna real: nunca tinha forçado de verdade a falha de **criação do arquivo de saída** (só malloc e pthread_create tinham sido forçados). Testei rodando `./mandelbrot 10 10 50 2` de dentro de um diretório com `chmod 555` (sem permissão de escrita) | OK de primeira — stdout vazio, `exit=1`, stderr `mandelbrot: falha ao criar arquivo de saida 'mandelbrot_afsr_serial.pgm': Permission denied` | — | 02/09/2026 18:1x | `scratchpad/test_output_file_fail.sh` |
| 20 | S1 | `evidencias.log` (padrão do `processflow`) tinha ficado de fora — só percebi depois que o projeto já estava pronto, não dava mais pra ter capturado "desde o início" sem inventar um histórico falso. Solução honesta: abri uma sessão real com `script` agora e reexecutei ao vivo os comandos de verificação mais importantes (build limpo, `make test`, `git clone` + `make test` numa pasta separada, ASan/UBSan em 5 tamanhos, empacotamento `afsr.tar` + reteste a partir do `.tar` extraído) — deixando uma nota bem clara no topo do arquivo dizendo que é uma captura consolidada de hoje, não um registro contínuo desde a Fase 0 (esse histórico real, fase a fase, já está neste diário desde a entrada 1) | OK — 230 linhas, tudo com comando real + saída real; primeira tentativa saiu com códigos de escape de terminal no meio do `git log` (cor/paginador do git sendo capturados pelo `script`); corrigido com `git --no-pager log --oneline --color=never`; segunda geração saiu limpa (`grep -c $'\x1b' evidencias.log` = 0) | `git log` colorido/paginado atravessou o `script` como bytes brutos de terminal, ilegível num PDF | 02/09/2026 17:3x | `evidencias.log` (não versionado, fica só na sua cópia local — é matéria-prima pro relatório, igual no `processflow`) |
